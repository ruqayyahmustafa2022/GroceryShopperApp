package com.detroitlabs.GroceryShopperApp.model;

import java.io.FileReader;
import java.io.IOException;
import java.util.Iterator;
import java.util.Map;

import org.json.simple.JSONArray;
import org.json.simple.JSONObject;
import org.json.simple.parser.*;

public class ReadStoreJson {/*
    // parsing file "JSONExample.json"
    Object obj = new JSONParser().parse(new FileReader("vegetables.json"));

    // typecasting obj to JSONObject
    JSONObject productJson = (JSONObject) obj;

    // getting limit
    String limit = (String) jo.get("limit");

    //System.out.println(limit);

    // getting age
   // long age = (long) jo.get("age");
   //     System.out.println(age);

    // getting address
  //  Map address = ((Map)jo.get("address"));

    // iterating address Map
  /*  Iterator<Map.Entry> itr1 = address.entrySet().iterator();
        while (itr1.hasNext()) {
        Map.Entry pair = itr1.next();
        System.out.println(pair.getKey() + " : " + pair.getValue());
    }

    // getting phoneNumbers
    JSONArray ja = (JSONArray) jo.get("phoneNumbers");

    // iterating phoneNumbers
    Iterator itr2 = ja.iterator();

        while (itr2.hasNext())
    {
        itr1 = ((Map) itr2.next()).entrySet().iterator();
        while (itr1.hasNext()) {
            Map.Entry pair = itr1.next();
            System.out.println(pair.getKey() + " : " + pair.getValue());
        }
    }

    public ReadStoreJson() throws IOException, ParseException {
    }
}
*/

}
